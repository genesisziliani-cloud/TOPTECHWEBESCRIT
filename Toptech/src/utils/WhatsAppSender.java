package utils;

public class WhatsAppSender {
    public static void send(String phone, String message) {
        // Aquí va la integración real (Twilio, Chat-API, etc)
        System.out.println("Enviando WhatsApp a " + phone + ": " + message);
    }
}